# --- core runtime -----------------------------------------------------------
streamlit>=1.36,<2.0        # UI; column_config + st.toggle need >=1.36
httpx>=0.27,<1.0            # HTTP client for DexScreener / GoPlus
pandas>=2.0,<3.0            # tables for scanner, holders, history
python-dotenv>=1.0,<2.0     # loads .env for optional API keys

# --- dev / test (safe to omit in production) --------------------------------
pytest>=8.0,<10.0

# --- optional LLM providers -------------------------------------------------
# Install only what you plan to use, then set MEMEDD_LLM_PROVIDER in .env.
# anthropic>=0.40           # Claude   (MEMEDD_LLM_PROVIDER=anthropic)
# openai>=1.40              # GPT      (MEMEDD_LLM_PROVIDER=openai)
#                           # Grok too (MEMEDD_LLM_PROVIDER=xai, same SDK)
