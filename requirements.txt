# --- core runtime -----------------------------------------------------------
streamlit>=1.36,<2.0        # UI; column_config + st.toggle need >=1.36
httpx>=0.27,<1.0            # HTTP client for DexScreener / GoPlus
pandas>=2.0,<3.0            # tables for scanner, holders, history
python-dotenv>=1.0,<2.0     # loads .env for optional API keys

# --- dev / test (safe to omit in production) --------------------------------
pytest>=8.0,<10.0

# --- optional LLM providers -------------------------------------------------
# Needed only for the narrative layer and the multi-LLM ensemble. Install the
# ones you have keys for; providers without a key or SDK are skipped with a
# reason shown in the sidebar, and the app runs fine without any of them.
#
#   anthropic>=0.40         # Claude          -> ANTHROPIC_API_KEY
#   openai>=1.40            # GPT             -> OPENAI_API_KEY
#                           # and Grok (xAI)  -> XAI_API_KEY (same SDK,
#                           #   pointed at https://api.x.ai/v1)
#
# For the full three-model ensemble:
#   pip install anthropic openai
